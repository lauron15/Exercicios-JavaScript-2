const nome = ["marcos","thiago", "Lauro", "Matheus"]; 
nome.forEach(nome => {
console.log("O nome é:" + nome);
}); 

// Esse for.Each, vai rodar o código, e cada vez que ele rodar, irá apresentar um nome. Rodou a primeira vez, o nome é Marcos, rodou a segunda, o nome éthiago. 
// e por ai vái, até finalizar. 


var numero = [1,2,3,4,5,6,7,8,9];
numero.forEach(numero => {console.log("Seu número é:" + numero )
}); 
  